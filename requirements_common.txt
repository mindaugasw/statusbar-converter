PyYAML==6.0.3
types-PyYAML~=6.0.12
pyinstaller==6.20.0
requests==2.34.2
types-requests~=2.33.0
# Pillow is needed for pyinstaller to be able to convert .png image to .icns to allow to use as macOS file icon
Pillow==12.2.0
# dearpygui 2.2.0, 2.3.0 hangs when closing any UI window. So keeping older version for now
dearpygui==2.1.1
# dearpygui_ext 2.1.1 causes error when instantiating UI with dearpygui < 2.3.0. So keeping older version for now
dearpygui_ext==2.0.0
parameterized==0.9.0
coverage==7.14.1
mypy==2.1.0
mypy_extensions~=1.1.0
ruff==0.15.15
